/**
 * Upload data to ipfs and returns file URL
 * @todo
 * @param {String} data -- base64 encoded data
 * @returns {String} -- fileURL
 */
export async function upload(data) {
    return "http://localhost";
}